import React from 'react';
export const metadata = {
    title: 'about',
    description: "mern stack developer",
  
  };
const page = () => {
    return (
        <div>
            this is about
        </div>
    );
};

export default page;